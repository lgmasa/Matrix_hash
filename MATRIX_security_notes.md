# MATRIX ハッシュ関数の安全性解析ノート

Kodera–Nogami の **MATRIX**（Mixed-basis Architecture using Towering Field
Representations for Extendable Grøstl State Blocks）に対する、単一トレイル差分攻撃
およびリバウンド攻撃の耐性解析のまとめ。Grøstl をベースに、状態を
$\mathbb{F}_{p^{16}} = \mathbb{F}_{(p^4)^4}$ の 1 元として扱う設計を前提とする。

---

## 0. 前提とパラメータ

- 状態: $4\times4$ 行列、各要素は $\mathbb{F}_p$ の元
- 素数: メルセンヌ素数 $p \in \{2^7-1,\ 2^{31}-1,\ 2^{127}-1\}$
- 現在の設定: $p = 2^{31}-1$、**格納長 $l = 16 \times 32 = 512$ bit**、**出力長 $n = 256$ bit**
- SubBytes: 状態全体を $\mathbb{F}_{p^{16}}$ の 1 元とみなして逆元 $x^{-1}$ をとる
  （**巨大単一 S-box**）+ アフィン変換
- 圧縮関数 $f$ と出力変換 $\Omega$ は **Grøstl からそのまま継承**
- ラウンド $R = \mathrm{MixBytes}\circ\mathrm{ShiftBytes}\circ\mathrm{SubBytes}\circ\mathrm{AddRoundConstant}$

> **重要な注意（実効状態長）**: $p = 2^{31}-1$ の元は $2^{31}-1$ 通りなので、
> 実効状態空間は $p^{16} \approx 2^{496}$。格納長 512 bit より約 16 bit 小さい。
> これは後述の $l \ge 2n$ 条件にわずかに影響する。

---

## Part 1. 単一トレイル差分攻撃の無意味性

### 1.1 確率を生むのは SubBytes だけ

AddRoundConstant（定数加算）・ShiftBytes・MixBytes は全て
$\mathbb{F}_p$-線形な全単射で**差分を確率 1 で保存**する。
差分確率を生むのは SubBytes の逆元写像のみ。しかもそれは状態 16 要素を
まとめて $\mathbb{F}_{p^{16}}$ の 1 元として反転する。

$$
\mathrm{DP}_{\text{trail}}(r) \le \left(\mathrm{maxDP}_{\text{inv}}\right)^r
$$

### 1.2 maxDP = 4/p^16

$q = p^{16}$、$I(x) = x^{-1}$（$I(0)=0$）とすると、$a\neq0$ に対し
$I(x+a) - I(x) = b$ の解数は：

- $x \notin \{0, -a\}$ では $bx^2 + abx + a = 0$ の 2 次方程式で**高々 2 解**
- $x = 0,\ -a$ は差分 $b = 1/a$ のときだけ解になり、**2 解追加**
- $b = 1/a$ のとき判別式は $-3a^2$。$-3$ が平方剰余なら 2 解追加され、最大 **δ = 4**

$16$ は偶数なので $p \neq 3$ の任意の素数で $p^{16}\equiv 1 \pmod 3$、
よって $-3$ は必ず $\mathbb{F}_{p^{16}}$ の平方剰余。したがって

$$
\boxed{\ \mathrm{maxDP}_{\text{inv}} = \frac{4}{p^{16}} = \frac{4}{q} = 2^2 \cdot 2^{-l}\ }
$$

アフィン変換は加法的全単射で差分スペクトルを保存するため、これが S-box 全体の maxDP。

### 1.3 理想値との比較

$l$ ビット置換の理想的な差分確率は $\approx 2^{-l}$。MATRIX は **1 ラウンドで既に**
$\mathrm{DP} \le 4 \cdot 2^{-l} = 2^{-(l-2)}$、理想値との差はたった $2^2$ 倍。
「状態全体を 1 回反転」することで、逆元の差分一様性 4 が場全体 $p^{16}$ に
まるごと薄まった結果。**多数の小 S-box のアクティブ数を数える wide-trail
論法とは構造が根本的に異なる。**

### 1.4 汎用攻撃との比較（p = 2^31-1, l = 512, n = 256）

| 量 | 値 |
|---|---|
| 1 ラウンド maxDP | $4/p^{16} \approx 2^{-494}$ |
| 単一トレイル差分の計算量 | $\approx 2^{494}$ |
| **汎用衝突攻撃（防衛ライン）** | $2^{n/2} = 2^{128}$ |

$$
\underbrace{2^{494}}_{\text{単一トレイル}} \;\ggg\; \underbrace{2^{128}}_{\text{汎用衝突}}
$$

1 ラウンドですら $2^{494}$ を要し、汎用衝突を $2^{366}$ 倍上回る。
ラウンドを重ねれば $2^{494r}$ とさらに指数的に悪化。
**単一トレイル差分攻撃は完全に無意味。∎**

---

## Part 2. 背景知識の整理（Q&A で確認した論点）

### 2.1 汎用攻撃（generic attack）とは

ハッシュ関数の内部設計を一切使わず、「入力を入れたら出力が返る箱」としてしか
見ない攻撃（ブラックボックス攻撃）。代表が誕生日攻撃。

| 攻撃 | 汎用複雑度（$n$ = 出力長） |
|---|---|
| 衝突 | $2^{n/2}$ |
| プリイメージ | $2^n$ |
| 第 2 プリイメージ | $2^n$ |

汎用攻撃は**出力長だけから決まる避けられない限界**であり、
「守るべき防衛ライン」。専用攻撃（差分・リバウンド）は、
このラインを**下回って初めて意味を持つ**。

### 2.2 汎用攻撃は実行可能か → 不可能

$2^{128} \approx 3.4\times10^{38}$。毎秒 $10^{18}$ 回計算しても約 **11 兆年**
（宇宙年齢の 800 倍以上）。物理法則（ランダウアー限界）からもエネルギー的に不可能。
→ だから攻撃者は「汎用より速い専用攻撃」を探し、設計者はそれが無いことを確認する。

### 2.3 「攻撃成功」の定義

**汎用攻撃より速い方法を見つけたら break**（実際に実行できなくても）。
たとえ $2^{120}$ でも $2^{128}$ より速ければ学術的には攻撃成功。
これは設計の想定が崩れた証拠であり、亀裂は往々にして広がる（MD5, SHA-1 が実例）。

### 2.4 衝突・プリイメージ・第 2 プリイメージ

| | 目標 | 出力の自由度 | 汎用コスト |
|---|---|---|---|
| 衝突 | $H(M)=H(M')$ の $M\neq M'$ | 何でもいい | $2^{n/2}$（易） |
| プリイメージ | 与えた $y$ に対し $H(M)=y$ | 固定（狙い撃ち） | $2^n$ |
| 第 2 プリイメージ | 与えた $M$ に対し $H(M')=H(M)$ | 固定 | $2^n$ |

衝突が最も安い（誕生日パラドックス）。だから**評価の基準線は常に衝突 $2^{n/2}$**。

### 2.5 衝突の悪用（署名偽造）

攻撃者はハッシュ値を指定できない。できるのは「同じハッシュになる文書ペア
$A$（無害）と $B$（悪意）を両方設計する」こと。正規手続きで無害な $A$ に
署名させ、その署名を $B$ に流用する。文書の自由領域や選択プレフィックス衝突を使う。
**実例**: Flame（MD5、偽 Microsoft 証明書）、SHAttered（SHA-1、衝突 PDF）。
人間が目視せず**機械がハッシュ値だけで自動処理する場面**（証明書、バイナリ）が本命。

---

## Part 3. wide-pipe（$l \ge 2n$）による構成の安全性

### 3.1 何を証明できるか

> **$P, Q$ が理想的な置換であると仮定すれば、この構成を使う限り、$n$ ビット
> ハッシュとして要求される安全性（衝突 $2^{n/2}$、プリイメージ $2^n$）が保証される。**

= **「組み立て方（構成）に穴がない」**ことの証明。
$P,Q$ の理想性は**仮定（前提）**であって結論ではない。逆は成り立たない
（$l\ge2n$ を満たしても $P=Q=$ 恒等写像なら壊れる）。

### 3.2 証明の 2 段構え

1. **数学的解析（本体、$l$ 建て）**: $P,Q$ 理想なら、ハッシュ衝突に最低 $2^{l/4}$、
   プリイメージに最低 $2^{l/2}$ の $P,Q$ 評価が必要。
   出典: Grøstl 論文 §4.1 → 原証明は Fouque–Stern–Zimmer, SAC 2008 (参考文献 [32])。
   indifferentiability は Andreeva–Mennink–Preneel, SCN 2010 (参考文献 [3])。
2. **翻訳（$l\ge2n$ を使う）**: $2^{l/4} \ge 2^{n/2} \iff l \ge 2n$。

$l \ge 2n$ は「$l$ 建ての保証を $n$ 建ての要求水準に橋渡しするピース」。
それ単独では何も保証しない。

### 3.3 要求水準は n ベースのみ

- **要求水準**（ゴール）: $n$ ベース。出力長から自動的に決まる。
- **$l$ ベースの数値**: 証明が与える保証（中間結果）。要求水準ではない。

### 3.4 なぜ $l$ が出るか

証明の舞台が**内部状態**（$l$ ビット）だから。ハッシュ衝突には
「内部衝突（$l$ ビット、反復途中の全 chaining value が対象）」または
「$\Omega$ での衝突（$n$ ビット）」の 2 経路があり、wide-pipe は前者
（誕生日攻撃 $2^{l/2}$）を後者（$2^{n/2}$）より高くして内部を狙う旨みを消す。

### 3.5 「$P,Q$ が理想か」は分からない

原理的に証明不可能（そもそも厳密には理想ではない）。分かるのは
「理想と区別する実行可能な方法が見つかっているか」だけ。
**Grøstl では削減ラウンドに非理想性が実際に見つかっている**
（積分 7R、ゼロサム分割 8R、リバウンド圧縮関数 6R）が、
ラウンド数マージンと wide-pipe でハッシュへの波及を防いでいる。
暗号の「安全」= 攻撃の失敗の蓄積による信頼。

### 3.6 現在の設定での数値（l=512, n=256）

| | 下限（保証） | 要求水準 | 判定 |
|---|---|---|---|
| 衝突 | $2^{l/4}=2^{128}$ | $2^{n/2}=2^{128}$ | ちょうど（等号） |
| プリイメージ | $2^{l/2}=2^{256}$ | $2^{256}$ | ちょうど（等号） |

$l = 2n$ ぴったりの設計。**実効 $l \approx 496$ では**下限が
$2^{124}$／$2^{248}$ に落ち、要求水準を僅かに割る（衝突で $2^4$、プリイメージで $2^8$ 倍）。

**対処（推奨）**: 正直に「格納長 512、実効状態 $p^{16}\approx2^{496}$、内部衝突は
$2^{248}\gg2^{128}$ で wide-pipe の目的は満たされる」と明記。厳密に条件を満たしたい
なら $n \le 248$（例 $n=224$）を選ぶ。これはメルセンヌ素数採用のトレードオフとして議論可。

### 3.7 「防衛ラインの決め方」は Grøstl 特有か → 普遍的

汎用攻撃を防衛ラインとする考え方、「汎用より速ければ break」の判定基準は
**暗号学全体の標準**（SHA-2, SHA-3, BLAKE, NIST SHA-3 コンペ要件）。
Grøstl 的なのは**根拠づけ方**（構成の証明可能安全性、wide-pipe、ラウンド数マージン）。
SHA-256 は narrow-pipe で証明も持たない。

---

## Part 4. リバウンド攻撃の耐性

### 4.1 リバウンド攻撃とは

2009 年提案の、AES 系ハッシュ向け差分攻撃。差分特性を 3 区間に分け 2 フェーズで処理：

- **インバウンド（真ん中を逆算で一発突破）**: 中央差分 $(\Delta_{\text{in}}, \Delta_{\text{out}})$
  を固定し、**各 S-box ごとに独立に DDT を引いて**それを満たす値を $O(1)$ で取得。
  アクティブ S-box が $k$ 個なら $\approx 2^k$ 個の starting point（弾薬）を大量生成。
- **アウトバウンド（外側を確率で伸ばす）**: 各 starting point を前後に確率
  $p_{\text{out}}$ で伝播。弾薬 $N$ 個に対し $N \cdot p_{\text{out}} \ge 1$ なら成立。

$$
\text{攻撃コスト} \approx \frac{1}{p_{\text{out}}}\quad(\text{インバウンドはほぼ無料})
$$

**成立の前提**: (1) 非線形層が多数の独立な小 S-box に分解できる、
(2) 十分な自由度（弾薬）がある。

### 4.2 Grøstl では有効か

**削減ラウンドでは有効（実際に破っている）、フルラウンドでは無効**。

| 攻撃対象 | 到達ラウンド | 全ラウンド |
|---|---|---|
| ハッシュ関数への衝突 | 3 | 10 / 14 |
| 圧縮関数への衝突 | 6（7 まで拡張可能性） | 10 / 14 |

差（10−3 等）が安全マージン。圧縮関数の衝突はハッシュ衝突に直結しない
（chaining value が制御外）。7R 超は自由度不足で困難。

### 4.3 MATRIX では有効になりうるか → 構造的に離陸不能

MATRIX の SubBytes は**巨大単一 S-box**なので、リバウンドの前提が両方崩れる：

- **前提 1 の崩壊**: S-box が 1 個しかなく、DDT を独立に引く操作が存在しない。
  中央差分を満たす解は $S(x+\Delta_{\text{in}})-S(x)=\Delta_{\text{out}}$ の解 = **高々 4 個**。
  $2^k$ の弾薬プールが作れない。
- **前提 2 の崩壊**: アウトバウンド 1 ラウンドのコストが $\approx 4/p^{16}=2^{-494}$。
  弾薬 $2^2$ に対し必要弾薬 $2^{494}$。**1 ラウンドすら払えない。**

**主張（Grøstl 型の「$X$ ラウンドまで」より強い）**:

> リバウンド攻撃の前提（十分な自由度を持つインバウンド解プールの構築）が
> 構造的に成立しないため、リバウンド攻撃は 1 ラウンドすら成立しない。

逆説的だが、Part 1 で差分に不利に見えた巨大単一 S-box が、リバウンドには盾になる。

### 4.4 実験的確認（本ノート付属コードの結果）

リポジトリ（lgmasa/Matrix_hash, MDS = circ(1,1,2,8)）の実 API に対して実行：

```
(A) maxDP:  固定差分ペアを満たす状態解は実測 1（追加解なし）。理論上限 4。
(B) 弾薬 N:  中央差分 (Δin,Δout) を満たす状態解 N ≈ 1〜4。
(C) 判定:   N ≤ 2^2  vs  必要 1/p_out ≈ 2^494  →  2^2 ≥ 2^494 は NO。離陸不能。
(D) 拡散:   1 成分だけの入力差分が SubBytes 通過後に 16/16 成分へ即座に拡散。
            → 「各 S-box を独立に扱う」前提が成立しないことの直接証拠。
```

**(D) が決定的**: 単一 S-box ゆえ、1 成分の差分が状態全体に広がり、
バイト単位で独立に制御する余地がない。

### 4.5 攻撃コストの測り方（道具立て）

| 指標 | 定義 | Grøstl | MATRIX |
|---|---|---|---|
| (a) 解プール $N$ | $\prod_{\text{active}}(\text{DDT 解数})$ | $\approx 2^{a}$ | $\le 2^2$ |
| (b) アウトバウンド確率 $p_{\text{out}}$ | $\prod(\text{各遷移確率})$ | 払える | $2^{-494}$/R |
| (c) 成立条件 | $N\cdot p_{\text{out}}\ge1$ | 削減 R で成立 | 不成立 |
| (d) トランケート差分 | active byte 数 + MDS 分岐数 | 分岐数 9 | **要 MDS 固定** |
| (e) 自由度勘定 | 状態 bit − 制約数 | 7R で枯渇 | 変換経路なし |

---

## Part 5. 未解決の課題（正直に併記すべき点）

1. **MixBytes / MDS 実体の確定**: 分岐数・完全拡散ラウンド数・ShiftBytes
   オフセットが未定。アウトバウンドの厳密なコスト計算には MDS の仮固定が必要
   （リポジトリは circ(1,1,2,8) を使用、Vandermonde-RS 系が候補）。
2. **ラウンド数・IV の解析**: 安全マージンを持つ $r$ の決定が未了。
3. **代数的攻撃こそ本命の懸念**: 単一逆元は $x^{-1}=x^{q-2}$ と代数的に単純。
   Gröbner 基底・補間・**積分/ゼロサム**（Grøstl で非理想性が出たのはここ）
   が差分・リバウンドより効く可能性。**優先的に検討すべき。**
4. **部分体・部分空間差分**: タワー体構造上、差分が $\mathbb{F}_{p^4}$ 部分体や
   不変部分空間に閉じ込められないかを明示的に排除する必要。

---

## Part 6. 論文での主張範囲（推奨する書き方）

- ❌「MATRIX は安全である」「$P,Q$ は理想置換である」→ 証明不可能、言い過ぎ
- ✅「MATRIX は $2^{128}$ の衝突耐性を**主張する**。単一トレイル差分は $2^{494}$、
   リバウンドは前提不成立により、いずれもこのラインを下回らない」
- ✅「構成の安全性は Grøstl から継承。本研究の課題は新設計の $P,Q$ が理想置換に
   近いことの傍証を積むこと」
- ⚠️「代数的攻撃・積分攻撃の解析は今後の課題」と正直に明記

**分業の明示が論文の論理を明快にする**：
構成（Grøstl から借用・証明済み）＋ 部品（新 $P,Q$ の暗号解析＝本研究）。

---

## 付録: 次のステップ

1. MDS 行列（circ(1,1,2,8) または Vandermonde-RS）と ShiftBytes を確定
2. トランケート差分パターンを構成し各ラウンド遷移確率を計算
3. インバウンド解プール $N\le2^2$、アウトバウンド $p_{\text{out}}\approx2^{-494}$ を明示
4. $N\cdot p_{\text{out}}\ll1$ より攻撃不成立、$2^{128}$ を下回れないと結論
5. 代数次数・ゼロサム分割の検討を優先課題として追加
