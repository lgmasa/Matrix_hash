# MATRIX リバウンド攻撃 実験コード 使い方

lgmasa/Matrix_hash リポジトリの実 API に合わせて書いた、リバウンド攻撃の
「離陸不能性」を実測するコードです。実際にビルド・実行して動作を確認済み。

## ファイル

- `16_rebound.c` — 実験本体（4 つのテスト A〜D）
- `16_rebound_main.c` — 単体実行用ドライバ（MDS を circ(1,1,2,8) にセット）

## ビルド方法

リポジトリのディレクトリに 2 ファイルを置いてから：

```bash
gcc -O2 -I. \
    16_rebound_main.c 16_rebound.c \
    16_matrix.c 16_fp16.c 16_fp4.c 16_fp.c 16_safety.c \
    -o rebound_test -lgmp -lm

./rebound_test
```

（GMP が必要: `apt-get install libgmp-dev`）

既存の `16_main.c` から呼びたい場合は、`16_header.h` に
```c
void run_rebound_experiments(const state_t *MDS, const affine16_t *AFF);
```
を追加し、`main()` 内で `run_rebound_experiments(&MDS, &AFF);` を呼べば OK。

## 4 つのテストの意味

| テスト | 測るもの | リバウンドとの関係 |
|---|---|---|
| **(A) maxDP** | $S(x+d)-S(x)=e$ の解数（理論上限 4） | maxDP = 4/p^16 を実測で裏付け |
| **(B) 弾薬 N** | 中央差分を満たす状態解の個数 | インバウンド解プール（Grøstl: 2^k、MATRIX: ≤4）|
| **(C) 判定** | $N$ と $1/p_{out}$ と $2^{128}$ の比較 | 成立条件 $N \cdot p_{out} \ge 1$ を判定 |
| **(D) 拡散** | 1 成分差分の SubBytes 通過後の広がり | 単一 S-box なら 16/16 に拡散（前提崩壊の証拠）|

## 実行結果（検証済み）

```
(A) maxDP:  実測解数 1（追加解なし）。理論上限 4 = maxDP 4/p^16 ≈ 2^-494
(B) 弾薬 N:  N ≈ 1〜4（≤ 2^2）。Grøstl の 2^(active bytes) と桁違いに小さい
(C) 判定:   2^2 ≥ 2^494 ?  →  NO。1 ラウンドすら払えない = 離陸不能
(D) 拡散:   1 成分の差分 → SubBytes 後 16/16 成分がアクティブ
```

### (D) が最重要

入力の 1 成分だけに差分を入れても、SubBytes を 1 回通しただけで
**状態全体（16/16 成分）に拡散**します。これは「各バイト S-box を独立に
DDT で扱う」というリバウンドの前提が MATRIX では成立しないことの、
実データによる直接証拠です。

## 設計上の注意点（コード内コメントにも記載）

1. **空間が 2^496 と広大**なので、ランダムサンプリングで「追加解」は
   まず見つかりません。それ自体が「1 つの差分ペアを満たす状態値が
   極少（≤4）」= 弾薬 N が小さいことの傍証になります。
   コードは既知解 x0 を必ずプールに含めた上で追加解を探す設計です。

2. **アフィン変換は差分に無関係**（加法的全単射）なので、差分解析では
   SubBytes の逆元部だけを見れば十分です。

3. **より厳密な解析には MDS 行列の固定が必要**。本コードは
   リポジトリの circ(1,1,2,8) を使用。トランケート差分による
   アウトバウンドコストの精密計算は、この MDS の分岐数に依存します。

## 次に拡張するなら

- **トランケート差分トレーサ**: どのバイトがアクティブかだけを追い、
  ShiftBytes + MixBytes(circ(1,1,2,8)) の分岐数を実測して、
  アウトバウンドのラウンドごとのコストを算出する関数を追加。
- **フルラウンド衝突探索の試行**: 実際に 2〜3 ラウンドで衝突を探す
  ブルートフォースを回し、汎用 2^128 と比較（当然届かないことを実証）。
- **代数次数の測定**: SubBytes の代数次数を測り、積分/ゼロサム攻撃の
  可能性（本命の懸念）を評価する関数。
